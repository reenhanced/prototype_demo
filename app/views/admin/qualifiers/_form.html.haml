= form_for [:admin, @qualifier] do |f|
  - if @qualifier.errors.any?
    =render 'shared/error_messages', :record => @qualifier

  .field
    =f.label :name
    =f.text_field :name

  .actions
    = f.submit 'Save'
